import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  layout: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  input: {
    flex: 17,
    height: 40,
    borderBottomWidth: 0,
    borderRadius: 0,
  },
  button: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    height: 40,
    borderBottomWidth: 0,
    borderRadius: 0,
  },
});
