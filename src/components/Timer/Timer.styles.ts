import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  timerContainer: {
    top: 100,
    alignItems: 'center',
    justifyContent: 'center',
  },
  remainingTime: {
    fontSize: 70,
  },
  cycleCount: {
    fontSize: 12,
  },
  icon: {
    width: 20,
    height: 20,
  },
  pomodoro: {
    fontSize: 24,
    marginTop: 12,
    alignItems: 'center',
  },
});
